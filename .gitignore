.patternitty/
# legacy misspelling — a pre-rename session's hook may still write here
.patternity/
__pycache__/
*.pyc
.DS_Store

# compile.py outputs, in case it's ever run with this repo as the cwd (dev/testing) —
# in a real target repo you'd commit these; here they're just pollution.
/CLAUDE.md
/AGENTS.md
/patternitty/
.cursor/rules/patternitty-learned.mdc
.github/instructions/
