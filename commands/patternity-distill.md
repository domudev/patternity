---
description: Distill captured signal into patternity's personal pattern store, promoting and compiling as needed
---

Run the `patternity` skill now: read `.patternity/signal.jsonl` and
`${PATTERNITY_HOME:-~/.patternity}/patterns/_SCHEMA.md`, update matching
patterns' `occurrences`/`state` (or create new `observed` ones), regenerate
`WALKING_DOC.md`, and immediately compile any pattern that newly reached
`proven`. Summarize what changed in one line per pattern.
