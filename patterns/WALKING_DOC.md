# Walking doc (template)

This is what `${PATTERNITY_HOME:-~/.patternity}/patterns/WALKING_DOC.md`
looks like once patternity has observed a few things. It's regenerated by
the `patternity` skill every time a pattern is added or its state changes —
don't hand-edit the list, edit the individual pattern files instead.

- [example](example.md) — observed, 1 occurrence — placeholder, delete once real patterns exist
